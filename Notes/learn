# react
	web storage and cookies
	fetch from other websites
	nextJs
	Cypress (end-to-end testing framework)
	ReasonML coding language (replace js?)

react native

pwa (progressive web app)
spa (single page app)
gitlab
automation

Gatsby (static site)
Headless CMS (strapi)
Jamstack?
